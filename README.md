# health-appointments-airflow-etl
Dockerized Airflow ETL that ingests fake hospital patient data from an API, generates synthetic appointments, loads them into PostgreSQL, and exposes analytics tables for a Power BI dashboard on providers, trends, and no-shows.
